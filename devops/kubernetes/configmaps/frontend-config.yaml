apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: ai-testing-platform
  labels:
    app: frontend
data:
  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"
  NEXT_PUBLIC_API_URL: "https://api.testing-platform.example.com"
  NEXT_PUBLIC_WS_URL: "wss://api.testing-platform.example.com"
